// You have to implement Polygon, Square and Triangle like in the Class exercise, but now using Constructor functions.
// Check out the specs defined in prototype.function.exercise.spec.js and make the tests pass.
// Tests command: npm t -- --watch prototype.function.exercise.spec

export function Polygon(label) {}

export function Square({ label, side }) {}

export function Triangle({ label, base, height }) {}
